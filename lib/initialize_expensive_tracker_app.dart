import 'package:my_projects/utils/common_exports.dart';

class InitializeExpensiveTrackerApp extends StatelessWidget {
  const InitializeExpensiveTrackerApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Expenses();
  }
}
